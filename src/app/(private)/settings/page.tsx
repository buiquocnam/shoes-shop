import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Settings - Shoe Shop',
  description: 'User settings',
};

export default function SettingsPage() {
  return (
    <main>
      <h1>Settings</h1>
    </main>
  );
}

